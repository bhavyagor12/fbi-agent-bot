import type { NextConfig } from "next";

const nextConfig: NextConfig = {

  // If using Next.js 16, it might be stricter about hosts in dev
  // but usually it doesn't show a Vite error.Blocked request. This host ("9459f719d236.ngrok-free.app") is not allowed.
};

export default nextConfig;
